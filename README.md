# CarPick Project 🚗

## 📌 프로젝트 소개

CarPick은 AI 기능을 탑재한 렌트카 서비스를 제공하는 웹 애플리케이션입니다.  
프론트엔드와 백엔드가 분리된 구조로 개발하며, 협업을 위해 명확한 브랜치 전략을 사용합니다.

## 🛠 기술 스택

### Frontend
- **Framework**: React + Vite
- **Language**: JavaScript (ES6+)
- **UI Library**: TailwindCSS
- **State Management**: React Query / Zustand
- **Build Tool**: Vite
- **Date Design**: React Date Range

### Backend
- **Framework**: Spring Boot
- **Language**: Java 21
- **Database**: MariaDB
- **ORM**: JPA (Hibernate) , Mybatis
- **Build Tool**: Gradle
- **API**: RESTful API, Swagger(OpenAPI) 문서화

---

## 🌳 브랜치 구조

```
main ← 배포 기준 (README.md, .gitignore만 존재)
│
└── dev ← 모든 개발의 기준 브랜치
     ├── dev-이주환
     ├── dev-이유채
     ├── dev-이경진
     ├── dev-임승우
     ├── dev-김동현
     ├── dev-나유진
     ├── dev-심성보
     ├── dev-장경건
     └── dev-최지현
```

- **main**: 배포용 브랜치, 직접 작업 금지
- **dev**: 개발 통합 브랜치, 모든 기능은 여기서 합쳐짐
- **dev-이름**: 팀원별 작업 브랜치, 프론트/백엔드 모두 가능

---

## 📖 Git 사용 가이드

### 1. 저장소 클론
```bash
git clone https://github.com/goentvy/CarPick.git
cd CarPick
```

### 2. 개인 브랜치 이동 
```bash
# 로컬 브랜치 확인
git branch
# 원격 브랜치 확인
git branch -r
# dev 브랜치로 이동 ( main 브랜치일 경우 )
git checkout dev
# 개인브랜치로 이동 ( dev 브랜치일 경우 )
git checkout dev-이주환
# 원격브랜치 생성
# 1. 로컬 브랜치 생성
git checkout -b dev-이주환
# 2. 원격 브랜치 생성
git push -u origin dev-이주환
```

### 3. 작업 흐름
- 개인 브랜치에서 작업 후 add & commit & push
- 작업 완료 시 `dev` 브랜치로 Pull Request 생성
- `base: dev`  `compare: dev-이주환`
- 코드 리뷰 및 테스트 후 `dev`에 병합
- 배포 시점에만 `dev` → `main` 병합

### 4. 자주 쓰는 명령어
```bash
# 변경사항 확인
git status

# 최신 dev 반영
# dev 브랜치로 이동
git checkout dev
# dev 원격 브랜치를 로컬브랜치로 불러오기
git pull origin dev
# 개인브랜치로 이동
git checkout dev-이주환
# 최신반영된 로컬 dev브랜치 코드 개인브랜치로 병합
git merge dev

# 커밋 & 푸시
git add .
git commit -m "feat: 기능 설명"
git push origin dev-이주환
```

## 📂 Git 관리 규칙

### 1. 브랜치 전략
- **main**: 배포 기준 브랜치, 직접 작업 금지
- **dev**: 모든 개발의 기준 브랜치
- **dev-이름**: 팀원별 작업 브랜치 (frontend/backend 모두 가능)

### 2. 커밋 메시지 컨벤션
- `feat:` 새로운 기능 추가
- `fix:` 버그 수정
- `chore:` 빌드/환경설정 변경
- `docs:` 문서 수정 (README 등)
- `refactor:` 코드 리팩토링
- `test:` 테스트 코드 추가/수정

예시:
```bash
git commit -m "feat: 로그인 페이지 UI 구현"
```

### 3. Pull Request 규칙
- 개인 브랜치 → dev 브랜치로 PR 생성
- 반드시 코드 리뷰 후 병합
- main 브랜치는 dev에서 충분히 테스트 후 병합

### 4. .gitignore 관리
- **frontend**: `node_modules/`, `dist/`, `.env` 등 무시
- **backend**: `target/`, `build/`, `.gradle/` 등 무시
- **Docker/CI/CD**: `docker-compose.override.yml`, `.env.docker`, 빌드 아티팩트(`*.zip`, `*.tar`) 무시
- **IDE/OS**: `.idea/`, `.vscode/`, `.DS_Store` 등 무시

→ 모든 브랜치에서 동일한 `.gitignore` 규칙을 적용

### 5. 협업 규칙
- 항상 `dev` 최신 상태를 개인 브랜치에 반영 (`git merge dev`)
- 충돌 발생 시 직접 해결 후 커밋
- 불필요한 파일/로그/빌드 산출물은 절대 커밋하지 않음
- PR은 작은 단위로 자주 올려서 리뷰 부담 최소화